import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const StyledBox = styled(Box)({
    color: "white",
    borderRadius: "15px",
    padding: "15px",
    marginTop: "15px",
    marginBottom: "5px",
  });